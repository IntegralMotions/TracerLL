[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "TracerLL"
version = "0.1.13"
description = "TracerLL: High-performance motor control library"
authors = [{ name = "William Kers and Jos Wigchert", email = "your@email.com" }]
license = { file = "LICENSE" }
readme = "README.md"
dependencies = []
requires-python = ">=3.6"
