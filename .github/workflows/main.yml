name: Build and Publish Wheels

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: "**/pyproject.toml"
      - name: Install dependencies
        run: |
          pip install -U pytest
          pip install -e '.[test]'
      - name: Run tests
        run: |
          pytest

  deploy:
    name: Build and Publish Wheels
    runs-on: ${{ matrix.os }}
    needs: [test]
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.13"]
    environment: release
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install -y build-essential gcc-multilib g++-multilib libc6-dev

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install llvm

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: "**/pyproject.toml"

      - name: Install dependencies
        run: |
          pip install -U twine setuptools wheel build cibuildwheel

      - name: Build Wheels
        run: |
          python -m cibuildwheel --output-dir dist

      - name: Publish Wheels
        env:
          TWINE_REPOSITORY: pypi
        run: |
          twine upload dist/*
